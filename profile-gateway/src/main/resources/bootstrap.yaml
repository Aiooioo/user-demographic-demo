server:
  port: 8081

spring:
  application:
    name: profile-gateway

  config:
    import:
      -

  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        enabled: true
      username: nacos
      password: nacos
    gateway:
      server:
        webflux:
          discovery:
            locator:
              enabled: true

